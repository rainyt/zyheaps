# iostools mac
--cmd arch -x86_64 g++ -o iostools.o -c macheaps.mm
--cmd arch -x86_64 g++ -shared iostools.o -o ../../templates/mac/App.app/Contents/Frameworks/iostools.hdll -framework Cocoa -lhl -L../../templates/mac/App.app/Contents/MacOS
